#include <bits/stdc++.h>

#include <algorithm>

#include <vector>

vector<int> nextPermutation(vector<int> &A, int n)

{

  //  Write your code here.

  next_permutation(A.begin(), A.end());

  return A;
}
